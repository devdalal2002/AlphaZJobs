FOLLOW-UP FIXES — navigation and language coverage

1. Fix desktop navigation (critical — pages are currently dead ends on desktop)

BottomNav.tsx has md:hidden, so it disappears entirely on screens ≥768px wide. Discover.tsx, Profile.tsx, Messages.tsx, and Rooms.tsx have no other way to navigate — once you're on any of these pages at desktop width, there's no way back except the browser's back button.
Add a persistent top navigation bar for md: and larger screens (a simple horizontal nav with the same links as BottomNav: Home, Discover, Profile, Messages, Rooms — plus the AlphaZJobs logo/name linking to /) shown only at md: and up, so mobile keeps BottomNav and desktop gets a top nav instead. Both should always be present depending on screen size — never neither.
Also add a persistent header/logo link back to /discover (or /) at the top of Messages.tsx and Rooms.tsx specifically, since those are the pages most likely to feel like a dead end mid-conversation.
2. Fix language toggle coverage

Right now only nav labels, 4 button labels, and 4 status messages are wired to the t translations object in LanguageContext.tsx — everything else (page headings, input placeholders, empty states, Settings dialog copy, About section) is hardcoded English regardless of language mode, so toggling the switch has almost no visible effect.
Expand the translations object in LanguageContext.tsx to cover: all page headings (Landing hero, Discover "Your AI Matches", Onboarding step titles, Rooms/Messages headers, Settings section titles), empty-state text ("No jobs found...", "No skills added yet...", "No people found..."), and key placeholder text (search inputs, message input).
Write the "Chronically Online" versions in the same slang voice as the existing strings (e.g. "What's your name?" → "who we talkin to?", "No jobs found. Try a different search." → "nothing's hitting rn, try again"), and the "English (US)" versions as the current plain-English equivalents.
Update each page component to read these new strings from t instead of hardcoding them.
Keep everything client-side, no new dependencies, don't restructure routing beyond adding the desktop nav.